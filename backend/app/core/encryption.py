"""Шифрование MTUCI/TECH-токена через Fernet.

Скелет — реализация в issue [BE-M1]. Ключ берётся из settings.token_encryption_key.
"""
# TODO [BE-M1]: encrypt_token(plain) / decrypt_token(cipher) на базе cryptography.fernet.
